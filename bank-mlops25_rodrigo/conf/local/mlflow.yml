server:
  mlflow_tracking_uri: "http://localhost:8080"
  mlflow_registry_uri: "http://localhost:8080"
  credentials: null
  request_header_provider:
    type: null
    pass_context: False
    init_kwargs: {}

tracking:
  disable_tracking:
    pipelines: []

  experiment:
    name: bank-mlops25-experiment
    restore_if_deleted: True

  run:
    id: null
    name: null
    nested: True

  params:
    dict_params:
      flatten: False
      recursive: True
      sep: "."
    long_params_strategy: fail

ui:
  port: "8080"
  host: "127.0.0.1"
